version: '3'

services:
  web:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8001:80
    volumes:
      - ./app/result/skud:/var/www/html
      - ./etc/apache_vhost.conf:/etc/apache2/sites-enabled/000-default.conf
      - ./etc/php.ini:/usr/local/etc/php/php.ini
